"""Valores por defecto de configuración.

Por compatibilidad, los defaults reales viven en `config.store`.
"""

from .store import (
    APP_NAME,
    APP_AUTHOR,
    CFG_NAME,
    CONFIG_FILE_PATH,
    DEFAULT_LECTURAS_PATH,
    DEFAULT_CONFIG,
    to_tags,
)
